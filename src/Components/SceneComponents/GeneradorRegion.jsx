import React, { useContext, useRef } from "react";
import { Context } from "../../Store/AppContext";

const GeneradorRegion = () => {
  const { store, actions } = useContext(Context);

    
  return <></>;
};

export default GeneradorRegion;
